#include "main.h"

/**
  * print_last_digit - Computes the absolute value of an integer.
  * @n: The digits.
  * Return: Always n%10
  */
int print_last_digit(int n)
{
	int lastDigit;

	lastDigit = n % 10;
	return (lastDigit);
}
