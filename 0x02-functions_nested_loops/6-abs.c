#include "main.h"

/**
 * _abs - compute the absulute value of the integer.
 * @ab_num: input integer numbers number
 * Return: Always x.
 */
int _abs(int ab_num)
{
	if (ab_num < 0)
	{
		return (-ab_num);
	}
	else
	{
		return (ab_num);
	}
}
