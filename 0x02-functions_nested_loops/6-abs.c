#include <main.h>
#include <stdio.h>

/**
 * _abs - compute the absulute value of the integer.
 * @ab_num: input integer numbers number
 * Return: Always x.
 */
int _abs(int ab_num)
{
	int x;

	x = abs(ab_num);
	return (x);
}

